# 文件管理 - 设计

## v1

### 1. 核心功能

#### （1）按扩展名分类

##### i. 获取后缀

Python 3 自带一个库 `pathlib.Path`，能简便地实现很多文件操作。语句 `Path(r"文件路径")` 会创建一个 Path 对象。

要提取文件后缀，可以使用 Path 对象的 `suffix` 属性；但要注意，这不能区分文件夹和文件，所以在此之前，必须用 Path 对象的 `is_file()` 方法判断出是文件；而要遍历所有的文件，可以用 Path 对象的 `iterdir()` 方法用序列列出所有的文件。

此外，提取文件后缀时，也需要注意将文件名全小写（用 `lower()` 方法），以避免文件将同一类文件由于大小写不同而分到不同的文件夹

##### ii. 创建文件夹

Path 对象的 `mkdir(mode=0o777, parents=False, exist_ok=False)` 方法会使用给定的路径新建目录。如果 *parents* 为假值（默认），则找不到的父级目录会引发 [`FileNotFoundError`](https://docs.python.org/zh-cn/3.14/library/exceptions.html#FileNotFoundError)。如果 *exist_ok* 为 false（默认），则在目标已存在的情况下抛出 [`FileExistsError`](https://docs.python.org/zh-cn/3.14/library/exceptions.html#FileExistsError)。我们可以使用 `target.mkdir(exist_ok=True) `（其中 target 为目标目录）来创建，这样无论该目录是否已被创建都不会报错。

另一个方法是使用 `os` 模块的 `makedirs()` 方法。语法为：`os.makedirs(path, mode=0o777, exist_ok=False)`，其中 mode（权限模式）默认值为此，exist_ok 和 Path 里面一样。这个方法的优势是支持多级创建（即如果要创建的目录的上一级...也没有创建，可以同步创建），而 mkdir() 方法只是单级创建。

（注意：os 模块中也有一个 mkdir() 方法，但是它的可操控性不如 Path 对象的，所以这里不介绍了）

##### iii. 移动文件

移动文件可以首先采用 `shutil` 模块，这是一个高级文件操作模块，主要用于文件或目录的复制、移动、删除、压缩、解压等操作，是对 `os` 模块的补充。这里，要移动可以使用 `shutil.move(src, dst)`。我们本次的 src 是一个文件，此时如果 dst 是一个已经存在的目录，文件会被移入其中；如果 dst 是一个文件路径，则 src 会被移动并重命名。会覆盖同名文件，需要手动处理（*加上后缀*）。

也可以考虑 `os.rename` 方法，支持在同一个磁盘/分区里移动文件。语法为 `os.rename("path/to/source.txt", "path/to/destination/new_name.txt")`。但是这样比较受限，不太推荐。

Path 对象也有 `rename()` 方法，用法类似 `source.rename(destination)`；另外还有 `replace()` 方法，也是移动文件，且会无条件覆盖目标路径下的同名文件。

##### 结合起来：

流程很短，用一个文件完全够了。

```Python
from pathlib import Path
import shutil

# 扩展名和目标文件夹名的对应关系：
CATEGORIES = {
    ".jpg": "图片", ".png": "图片",
    ".txt": "文档", ".md": "文档", ".pdf": "文档", ".docx": "文档",
    ".mp4": "视频", ".mkv": "视频",
}

def unique_target(dir: Path, file: Path) -> Path:
    """找到不冲突的目标路径，冲突时自动加后缀序号 _1、_2..."""
    target = dir / file.name
    n = 1
    while target.exists():
        target = dir / f"{file.stem}_{n}{file.suffix}"
        n += 1
    return target

def main(src: Path):
    for item in src.iterdir():
        if not item.is_file():
            continue
        
        suf = item.suffix.lower()
        if suf not in CATEGORIES:  # 跳过未分类的扩展名
            continue
            
        target_dir = src / CATEGORIES[suf]
        target_dir.mkdir(exist_ok=True)
        target = unique_target(target_dir, item)
        shutil.move(str(item), target)
        print(f"移动：{item.name} -> {CATEGORIES[suf]}/{target.name}")

if __name__ == "__main__":
    main(Path("D:/test"))
```

这部分在 Git 日志里记为：`feat: 自动分类文件，并能处理后缀冲突`

#### （2）确认提示

尽管当前在移动之后能够显示把东西移到了哪里，但是缺少保障。我们可以做一个确认提示（把显示功能放在移动操作之前），另外可以做一个日志，用以进行撤销等操作。这一部分做确认提示。

要做确认提示，就需要虚拟推演发生了什么。之前的结构是一边遍历一边找到应该移动到哪，并实际进行移动；而如果要虚拟推演，就需要把遍历和确认移动位置的部分单独拆出来，并修改 main() 函数。我们不妨把这部分单独做成一个函数 `plan(src: Path) -> list`：

```Python
def plan(src: Path) -> list:
    """算出每个文件应该放到哪里，返回一个 [(源, 目标, 分类), ...] 的列表，但不进行实际文件操作"""
    moves = []
    for item in src.iterdir():
        if not item.is_file():
            continue
        suf = item.suffix.lower()
        if suf not in CATEGORIES:
            continue
        target_dir = src / CATEGORIES[suf]
        target = unique_target(target_dir, item)
        moves.append((item, target, CATEGORIES[suf]))
    return moves
```

确认提示作为一个单独的函数：

```Python
def confirm_prompt(moves: list) -> bool:
    print(f"将移动 {len(moves)} 个文件：")
    for item, target, cat in moves:
        print(f"    {item.name} -> {cat} / {target.name}")
    while True:
        ans = input("确认执行？(y/n): ").strip().lower()
        if ans in ("y", "yes"):
            return True
        if ans in ("n", "no"):
            return False
        print("请输入 y 或 n")
```

事已至此，移动操作也可以单独写成一个函数：

```python
def execute_move(moves: list) -> None:
    for item, target, cat in moves:
        target.parent.mkdir(exist_ok=True)
        shutil.move(item, target)
        print(f"移动：{item.name} -> {cat}/{target.name}")
```

至此，main 函数也需要大修，从写满了各种操作到只需要缝合起来所有函数即可；此外，还给 main 函数加上了没有需要整理的文件的判定。

#### （3）日志

日志是纠错部分的第二步，记录下来的东西一方面可以给人看，用以确认，另一方面可以给撤销等操作做记忆凭证。
写日志的函数非常简单：

```Python
from datetime import datetime

def write_log(item: Path, target: Path, ts: str):
    with open(LOG_FILE, "a", encoding="utf-8") as f:
        f.write(f"{ts}\t{item.resolve()}\t{target.resolve()}\n")  # 记录移动的绝对路径
```

然后在 `execute()` 的 `shutil.move()` 后调用 `write_log(item, target)` ，并在 for 语句前加上 `batch_ts = datetime.now().strftime("%Y-%m-%d %H:%M:%S")  # 取时间` 即可。

（注意这里使得每一批次执行的操作的时间是相同的，以便将来撤回同一批次的操作）

#### （4）更健全的日志

如果日志用 json 文件存储会更好，这样的兼容性、扩展性都会大大升级。新的函数为 `write_log_json`。

#### （5）撤回操作

首先，必须明确我们要做的是一个什么样的撤回功能。我希望实现一个能撤回同一批次操作的功能。注意，这不是“快照式回滚”，而是将某一次的操作完全重做，而不考虑其他批次的操作。这样做的可行性来源于我们 *每一次操作都只将主文件夹中的文件放进子文件夹中，从而不会出现两次操作同一文件的情况，因此无需考虑对一个文件操作的上一步操作进行查找和撤回操作*。

因此，我们可以将操作大概划分为下面几个步骤：

```python
从日志读取操作() -> [((第一组移动前, 后), ...), ...]:
	...
按批次显示():
    ...
选取批次():
    ...
将一组文件按路径撤回去:
    ...
确认操作():
    ...
```

需要注意一些特殊情况：

- 有些文件可能被外界操作（例如移动、删除、重命名），导致虽然在日志中有记载，但是却无法找到对应的文件。由于无法将外界的操作也写入日志，所以对这种文件只好选择放弃。
- 进行撤销操作时，可能会出现原名字已经被占用的情况，这时依旧可以考虑加后缀的方法。

#### （6）手动输入要整理的文件夹目录

作为模块使用时，向 main 函数传参即可；作为脚本运行时，是写死的。可以修改一下，使得作为脚本运行时，用户也可以手动输入待整理的文件夹目录。

